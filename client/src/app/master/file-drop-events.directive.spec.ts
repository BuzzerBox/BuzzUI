import { FileDropEventsDirective } from './file-drop-events.directive';

describe('FileDropEventsDirective', () => {
  it('should create an instance', () => {
    const directive = new FileDropEventsDirective();
    expect(directive).toBeTruthy();
  });
});
